Khai báo API trong file configuration.ts
Các code dùng chung có thể tách thành component.
Nên sử dụng hooks để tái sử dụng code, tránh viết code trùng lặp.
Giao diện đơn giản, đủ dùng, không làm thêm bất kì tính năng sáng tạo nào, nếu chưa có yêu cầu.
Đảm bảo phong cách code giữa các file đã có là đồng nhất
Trước khi code mới phải tự hỏi code đã ngắn gọn nhất chưa, code có thể xử lí trong vài dòng không?
Không log bừa bãi, không comment thừa.